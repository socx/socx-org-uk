{
  "name": "socx-org-uk",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently -n api,web,worker -c cyan,green,yellow \"npm run dev --workspace=apps/api\" \"npm run dev --workspace=apps/web\" \"npm run start --workspace=apps/worker\"",
    "dev:api": "npm run dev --workspace=apps/api",
    "dev:web": "npm run dev --workspace=apps/web",
    "dev:worker": "npm run start --workspace=apps/worker",
    "build": "npm run build --workspace=apps/api && npm run build --workspace=apps/web"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
